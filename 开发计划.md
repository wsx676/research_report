# miniFARS 五天开发计划

> **依据**：《方案设计文档.md》（miniFARS 方案）
> **当前窗口**：第三轮提交期（08/21 ~ 09/10），本计划目标为 **5 天内产出一个完整可提交的 v1 包**
> **每日总原则**：当天结束前必须完成三件事——代码进 Git、metering 流水落盘、次日开工前 10 分钟走查昨日验收项

---

## 计划总览

| 天 | 主题 | 核心交付物 | 对应设计文档章节 |
|---|---|---|---|
| D1 | 地基：环境 + 工作区 + 编排骨架 | JiuwenSwarm 跑通、workspace 规范落地、`paper_pipeline` 空跑 | §4.1/4.2/6.1 |
| D2 | Ideation Swarm | 4 个子 Agent + 质量门 + 熔断器，产出 accepted_proposal.md；**PR1 提交** | §5.1、§7 |
| D3 | Planning + Experiment Swarm | experiment_contract.yaml schema、实验循环 + 技能库 + 有效性门 + 检查点；**PR2 提交** | §5.2/5.3、§7 |
| D4 | Writing Swarm + 第一篇论文 | blueprint.json、draft.tex → ICLR 模板 PDF 初稿 | §5.4、§8.2 |
| D5 | 预评循环 + 提交包定稿 | SAR 预评迭代定稿、全部提交文档、打包走查、正式提交 | §5.5、§9、§2.2 |

**五天结束时必须达到的状态**：一个按 §2.2 目录约定打包的 zip，含英文 ICLR 模板论文 PDF、对应 SAR Access Token、可复现代码、三份 docs、framework_contribution.md（≥2 个 PR 链接 + patch）、自动生成的 resource_report.md。

---

## D1 —— 地基：环境、共享工作区、编排骨架

**目标**：让 `python code/main.py --topic agent_context --dry-run` 能按四阶段顺序空跑并写入 workspace。

### 上午：环境与依赖
- [ ] clone JiuwenSwarm（atomgit.com/openJiuwen/jiuwenswarm），按 Quickstart 跑通官方示例
- [ ] 配置 MaaS 模型端点：**强模型**（假设生成/写作）与**轻量模型**（摘要/格式校验）各配一个，写入 config.yaml（§6.2 分级路由）
- [ ] 文献检索工具验证：arXiv API + Semantic Scholar API 各发一次真实查询
- [ ] LaTeX 工具链验证：ICLR 2026 模板能本地编译出 PDF
- [ ] SAR 平台（paperreview.ai）账号与 Access Token 机制确认，记录每日评测限额

### 下午：共享工作区 + 编排骨架
- [ ] 实现 workspace 初始化器：按 §6.1 目录规范建 `workspace/<project_id>/`，自动 `git init`
- [ ] 定义制品 schema v0：proposals/*.md 头部元数据、results/*.json + run_meta.json 字段、blueprint.json 骨架
- [ ] 实现顶层编排 skill `paper_pipeline`：四阶段串行调度 + 阶段间只传制品路径（§4.2）
- [ ] MeteringMiddleware 最小版：拦截 LLM 调用写 metering/ 流水（§6.2）——**D1 就要上线，否则后四天的资源数据缺失**

### 晚上：首个 PR 开工
- [ ] fork JiuwenSwarm，建分支 `feature/quality-gate`，写 QualityGate 组件接口与单测框架（§7 PR1）
- [ ] 写 topic.yaml（主题 A：Agent 上下文工程，含子方向关键词 + 算力预算）

### 验收标准
1. 官方示例跑通且能复述 JiuwenSwarm 的 skill/子 Agent 注册机制（写进 module_call.md 草稿）；
2. `--dry-run` 在 workspace 中留下四个阶段目录与 git 提交记录；
3. metering/ 中出现至少 3 条真实调用流水。

### 风险与预案
| 风险 | 预案 |
|---|---|
| JiuwenSwarm 环境装不通（依赖/昇腾相关） | 用 discussions 区提问 + 降级到纯 Python 封装层先跑通逻辑，环境适配放 D2 上午并行处理 |
| MaaS 端点限流 | D1 就把分级路由和缓存打开，避免 D4/D5 集中烧额度 |

---

## D2 —— Ideation Swarm（选题与假设生成）

**目标**：从 topic.yaml 自动产出过质量门的 accepted_proposal.md，并把质量门/熔断器作为 PR1 提交。

### 上午：SurveyAgent + HypothesisAgent
- [ ] SurveyAgent：检索子方向近 12 个月文献 → 生成摘要卡片（**压缩后才落盘**，§6.2 上下文压缩）+ 研究空白清单
- [ ] HypothesisAgent（Lead）：基于空白清单生成 5~8 个候选假设（问题陈述 + 可验证预测 + 最小实验）
- [ ] 全部候选写入 workspace/proposals/（含将被淘汰的，供审计）

### 下午：PeerAgent + GateAgent + 熔断器
- [ ] PeerAgent：新颖性查重（对 Survey 卡片做相似度/引用比对）+ 可行性打分，与 Lead 辩论 1~2 轮
- [ ] GateAgent：四维 rubric（新颖性/可验证性/算力可行性/边界清晰度）打分，阈值放行（§5.1）
- [ ] CircuitBreaker：迭代 ≥3 轮无假设过门 → 取最高分草案强制放行
- [ ] 端到端跑一次真实 Ideation，产出 accepted_proposal.md

### 晚上：PR1 提交
- [ ] 把 QualityGate + CircuitBreaker 整理为对 JiuwenSwarm 源码的通用组件（配置化 rubric、评审记录落盘）
- [ ] 附单测 + 非科研场景示例，**提交 PR1**；同步导出 patch 文件备份（§7 兜底策略）

### 验收标准
1. accepted_proposal.md 含：中心假设、可量化预测、最小实验描述、GateAgent 评分记录；
2. 熔断路径至少被单测覆盖一次（构造"全不达标"输入）；
3. PR1 已提交，patch 文件入库。

### 风险与预案
| 风险 | 预案 |
|---|---|
| 假设质量差、全被门拒掉 | 检查空白清单质量；必要时人工微调 topic.yaml 子方向关键词（半流程允许，但记录干预日志） |
| 文献 API 检索结果噪声大 | SurveyAgent 加"近 12 个月 + 引用数下限"过滤器 |

---

## D3 —— Planning + Experiment Swarm（实验链）

**目标**：accepted_proposal.md → 契约 → 真实实验结果落盘，含有效性门与断点续跑。**这是五天中工作量最大的一天，实验设计在上午就要冻结。**

### 上午：Planning Swarm
- [ ] experiment_contract.yaml schema 定稿（§5.2 五类任务 + budget 字段），写 schema 校验器
- [ ] PlannerAgent：把 accepted_proposal 翻译为契约；**强制校验 baselines ≥ 2、ablation ≥ 1**
- [ ] 人工过目契约的实验可行性（这是论文实验设计的冻结点）

### 下午：Experiment Swarm 执行引擎
- [ ] Orchestrator：按契约顺序执行任务，每任务 = 生成代码 → 沙箱执行 → 指标落盘 results/*.json + run_meta.json
- [ ] 策展技能库 v1：benchmark runner、多 seed 并行、matplotlib 统一风格绘图、LLM-as-a-Judge 评分器四个模板（§5.3）
- [ ] 有效性评估门：main 完成后对照阈值，失败 → 跳过 analysis、负结果保留
- [ ] CheckpointManager：每任务完成即 git commit；杀掉进程验证断点续跑

### 晚上：真实实验跑批 + PR2
- [ ] 主题 A 实验全量跑完（预算硬约束：契约 budget 超限自动降级）
- [ ] 把 CheckpointManager + MeteringMiddleware 整理为 **PR2 提交**（含 patch 备份）

### 验收标准
1. results/ 中每个结果都有 run_meta.json（命令/seed/模型版本/token/时间戳）；
2. 有效性门两条路径（通过/早停）都被真实或模拟数据覆盖；
3. 杀进程再续跑，能从断点恢复且 git 历史无丢失；
4. PR2 已提交。

### 风险与预案
| 风险 | 预案 |
|---|---|
| 实验超时/超预算 | D3 上午冻结契约时就按"单机 2 小时内跑完"设计实验；LLM-as-a-Judge 样本数先小后大 |
| 主实验不支持假设 | 不慌——按 §5.3 走 negative result 路线，短论文+可证伪负结果在 SAR 有好评先例（FARS FA0328） |
| 生成实验代码反复报错 | 强制只用技能库模板组合，禁止 Agent 从零写评测逻辑 |

---

## D4 —— Writing Swarm + 第一篇论文

**目标**：制品包 → blueprint.json → ICLR 模板英文论文 PDF 初稿，全链路证据可追溯。

### 上午：AnalysisAgent（证据审计）
- [ ] 审计 proposal/contract/results 三类制品，产出 blueprint.json：每条 claim 显式链接 {source_artifact, 图表候选, 支撑强度}（§5.4 Step1）
- [ ] 硬规则：**无证据支撑的 claim 拒绝入蓝图**
- [ ] 图表生成：分析图用绘图脚本在 results 上执行产出（图数=真实数据）；方法示意图按图注生成

### 下午：DraftAgent + FormatAgent
- [ ] DraftAgent：按 ICLR 结构逐节写作，每节只喂蓝图对应证据；引用只取自 Survey 文献卡片，CitationChecker 逐条校验存在性
- [ ] SAR 导向写作规范落地（§8.2）：前两段讲清唯一中心贡献、Limitations 节坦白实验边界、candid analysis 风格
- [ ] FormatAgent：LaTeX 编译 + ICLR 模板合规 + BibTeX 校验 + **数值一致性审计**（摘要/正文/表格同值扫描）
- [ ] 编译产出 paper_v1.pdf

### 晚上：内部初审 + PR3 开工
- [ ] 人工通读一遍，对照 §8.2 五条规范逐项打勾
- [ ] AcademicFormatValidator（§7 PR5）开工：把下午的校验逻辑抽成通用组件

### 验收标准
1. blueprint.json 中 claim 与制品的链接 100% 可点开核对；
2. paper_v1.pdf 编译无错误、符合 ICLR 模板、全部引用真实存在；
3. 一致性审计零告警。

### 风险与预案
| 风险 | 预案 |
|---|---|
| 写作出现过度声称 | 回蓝图层删 claim，不允许在 DraftAgent 层"打补丁"（证据优先原则不破） |
| LaTeX 编译问题积压 | FormatAgent 每节编译一次，不等到全文完成 |

---

## D5 —— 预评循环、提交包与正式提交

**目标**：SAR 预评迭代定稿 → 按 §2.2 清单打包 → 完成本轮正式提交。**SAR 有每日限额，上午第一件事就是提交预评。**

### 上午：SAR 预评循环
- [ ] 用 Access Token 提交 paper_v1 预评（错峰，早间提交）
- [ ] RebuttalAgent 解析意见：只处理低风险高收益项（补实验说明/修图表/调 claim 强度），不重写方法（§5.5）
- [ ] 迭代 ≤2 轮 → 定稿 paper_final.pdf → **提交正式 SAR 评测，记录与论文对应的 Access Token**

### 下午：提交文档与资源报告
- [ ] 按 §9 映射表产出三份 docs：architecture.md（§4+§5 精简）、module_call.md（实际调用的 JiuwenSwarm 模块清单）、innovation.md（三大创新点）
- [ ] resource_report.md：由 metering/ 流水自动生成（阶段×Agent token 明细、时长、总成本）
- [ ] framework_contribution.md：PR1/PR2（+PR3 若有）链接 + patch 文件 + 兼容性说明
- [ ] code/run_guide.md：干净环境复现步骤；提交说明.md：目录树 + 逐项说明

### 晚上：走查 + 正式提交
- [ ] 干净环境复现演练一次（clone 代码 → 按 run_guide 跑通小样例）
- [ ] 对照 §2.2 清单逐项走查：关键文件名（paper.pdf / PaperReview-AccessToken.txt / architecture.md / module_call.md / innovation.md / framework_contribution.md / resource_report.md）一个不能错
- [ ] 打包 团队名称.zip → **平台正式提交**（当日第 1 次提交，预留 2 次额度给意外修复）

### 验收标准
1. SAR 正式评测完成，Token 与 paper_final.pdf 一一对应；
2. zip 内文件清单与 §2.2 完全一致；
3. 干净环境复现通过；
4. 提交成功且平台无格式/编码报错（注意 UTF-8 无 BOM）。

### 风险与预案
| 风险 | 预案 |
|---|---|
| SAR 当日限额用尽 | D4 晚就提交预评抢额度；正式评测若当日失败，次日（仍在窗口内）补测，今日只提交代码包占位 |
| 打包文件名错误被平台判格式错误 | 走查用脚本自动比对文件名，不靠肉眼 |
| 提交后发现问题 | 每日 3 次额度仅用 1 次，剩余额度当天修复重交 |

---

## 跨天事项

### 资源与预算红线
- 全程 token 预算建议上限：**D1~D3 开发调试 ≤ 150 万 token/天，D4~D5 论文产出 ≤ 300 万 token/天**，由 MeteringMiddleware 每日对账；
- 实验 GPU/CPU 时间按契约 budget 硬约束，超限任务自动降级。

### 与后续轮次的衔接（本轮结束后的方向）
| 后续窗口 | 动作 |
|---|---|
| 第四轮（09/11~09/30） | 换 topic.yaml 跑主题 B（记忆引擎）验证跨主题泛化；开发 `--mode long` 长文模式；PR3/PR4/PR5 补齐 |
| 第五轮（10/01~10/09） | 三轮分数对比定最优主题；定稿冲刺；截止前 2 天完成 SAR 正式评测 |
| 决赛（10/09~10/31） | 复现材料、长文+泛化测试专项优化、代码开源至 agent-store |

### 每日站会检查项（10 分钟）
1. 昨日验收项是否全部打勾？未完成的降级或转移了吗？
2. metering 昨日消耗是否超红线？
3. 今日唯一"必须完成"项是什么？（D1 骨架 / D2 proposal / D3 results / D4 PDF / D5 提交）
4. PR 状态：已提交几个、有无 review 意见需要响应？

---

*计划版本：v1.0 ｜ 对齐《方案设计文档.md》v1（405 行版）｜ 制定日期：2026-08-25*
