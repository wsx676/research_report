# 参赛方案设计文档

> **赛题**：CCF BDCI 2026 ·【华为 openJiuwen】基于 JiuwenSwarm 的 Agent 科研论文自动生成
> **方案代号**：miniFARS —— 基于 JiuwenSwarm 的可审计四阶段科研流水线
> **赛程**：2026-06-23 ~ 2026-10-31（初赛五轮提交，TOP5 入围决赛）
> **官方参考标杆**：FARS（Fully Automated Research System，arXiv:2606.31651）

---

## 1. 方案总览

### 1.1 一句话定位

在 openJiuwen / JiuwenSwarm 蜂群框架之上，复刻并轻量化 FARS 的"四阶段流水线 + 共享可审计工作区 + 证据优先写作"架构，端到端自动生成符合 ICLR 模板的 Agent 方向英文科研短论文，并以 Stanford Agentic Reviewer（下称 SAR）评分为核心优化目标。

### 1.2 设计总原则（全部来自 FARS 的可借鉴特征）

| FARS 特征 | 本方案对应实现 |
|---|---|
| 阶段化解耦（Ideation→Planning→Experiment→Writing） | 四个 team skill / 子 Agent 分工，输入输出以文件契约定界 |
| 共享工作区 = 项目记忆 + 制品存储 | `workspace/` 目录 + Git 版本化，全程制品留痕 |
| 假设驱动 + 负结果诚实报告 | 每篇论文 = 一个边界清晰的假设 + 可靠验证，失败结果照写 |
| 自动质量门 + 熔断器 | Ideation 出口设评审 Agent；迭代超限自动取最优草案 |
| 实验契约（五类任务分解） | Planning 产出机器可读的 experiment contract |
| 证据与写作分离（Analysis Agent + Paper Blueprint） | Writing 前先做证据审计，论断逐条链接到制品 |
| 检查点式容错 | 每个任务完成即提交，中断后断点续跑 |
| 资源可审计 | 全链路 Token/时长计量中间件，自动生成 resource_report.md |

### 1.3 与评分维度的对应关系（设计的指挥棒）

| 初赛评分维度 | 分值 | 本方案得分策略 |
|---|---|---|
| 论文质量：SAR 官方评分 | 40 | FARS 式证据链写作 + 消融/基线完备性 + 提交前 SAR 预评循环 |
| 论文质量：论文评测 Agent 评分 | 20 | 用 SAR token 自建预评流水线，风格对齐 ICLR 审稿规范 |
| Agent 系统能力 | 15 | 四阶段架构文档 + 可复现源码 + 质量门/熔断/容错设计 |
| 资源消耗 | 10 | 计量中间件 + 上下文压缩 + 缓存 + 分级模型路由 |
| openJiuwen 贡献 | 15 | 向 JiuwenSwarm 提交 PR（容错、计量、学术校验插件） |

决赛额外增加"长文生成与跨主题泛化测试（20 分）"——架构必须**主题参数化**（topic 作为配置输入而非硬编码），这是第 4 节流水线设计的硬约束。

---

## 2. 赛题要求解读

### 2.1 任务要求

- 完成**指定 Agent 方向**科研短论文的全流程/半流程自动生成：选题拆解 → 文献调研 → 方法设计 → 论文撰写 → 结果分析。
- 建议主题（三选一或多选）：**Agent 上下文工程设计 / Agent 记忆引擎设计 / Agent 自演进**。
- 输出：英文、ICLR 模板、篇幅不限；建议接入 SAR 平台自动评测。
- **有效性硬门槛**：参赛 Agent 必须基于 openJiuwen 开源代码开发，且**必须修改 JiuwenSwarm 源码**，否则作品无效。

### 2.2 提交物清单（与 `提交样例/` 目录一一对应）

```
团队名称.zip
└── 团队名称/
    ├── paper/paper.pdf                        # 英文，ICLR 模板，1 篇
    ├── AgenticReviewer/PaperReview-AccessToken.txt   # 与 paper.pdf 对应
    ├── code/                                  # Agent 源码（main.py/config.yaml/
    │                                          #   requirements.txt/run_guide.md）
    ├── docs/
    │   ├── architecture.md                    # 架构设计
    │   ├── module_call.md                     # openJiuwen 模块调用说明
    │   └── innovation.md                      # 创新点说明
    ├── framework_contribution.md              # 框架贡献 + PR 链接
    ├── resource_report.md                     # Token/时长统计（可追溯）
    └── 提交说明.md                             # 目录结构与简要说明
```

### 2.3 关键约束与红线

1. **每日每队最多 3 次提交**，以每轮最后一次提交为准 → 每轮只在"最佳候选版本"确认后提交 1~2 次。
2. **SAR 有每日评测数量上限** → 预评要错峰、限流，最终版论文至少提前 2 天完成 SAR 评测拿到 token。
3. 竞赛数据仅限训练用途；作品须原创；决赛需现场复现 → 全链路必须可在单机复现（不依赖私有集群）。
4. 获奖后代码须开源至 gitcode.com/openJiuwen/agent-store → 代码从第一天就按开源标准写（注释、license、脱敏）。
5. 初赛按"三轮排行中的最高得分"排序 → 允许用不同轮次试不同论文主题，取最优。

---

## 3. FARS 架构的可借鉴特征清单

基于 arXiv:2606.31651 与官方部署资料，筛选出**与本赛题直接相关**的 10 个特征：

1. **四阶段流水线**（Ideation / Planning / Experiment / Writing），阶段间以文件制品交接，异步松耦合。
2. **共享可审计工作区**：假设文档、计划、代码、日志、草稿、终稿全部持久化，既是记忆又是审计证据。
3. **Ideation 质量门 + 熔断器**：提案过自动评审才放行；迭代反复失败则取当前最佳草案，防止无限循环烧 Token。
4. **实验契约**：Planning 输出层次化、机器可读的计划，分解为环境搭建 / 基线实验 / 主实验 / 有效性评估 / 分析实验五类任务。
5. **有效性评估门**：主实验不支持假设时提前终止分析实验（省算力），但负结果完整保留进论文。
6. **策展技能库**：预置已验证的代码模式（数据加载、评测脚本、绘图），降低实验代码错误率。
7. **托管式算力抽象**：智能体声明意图（"跑一个 3 seed 的消融"），底层负责调度，Agent 不碰 infra。
8. **检查点容错**：任务级提交 + 断点续跑 + 终止清理，支撑长时间无人值守运行。
9. **证据优先写作**：Analysis Agent 先审计上游制品生成 Paper Blueprint（论断↔证据逐条映射），Writing Agent 再行文，防止"机会主义论断"与结果幻觉。
10. **资源可审计性**：token、时长、成本全程记录（FARS 部署：417h / 216 亿 token / 166 篇）。

**不照搬的部分**：160 GPU 集群、全闭源模型统一端点、GitLab 直播——替换为"单机 + MaaS API + 本地 Git"的轻量等价物（见 §6）。

---

## 4. 总体架构设计

### 4.1 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                     miniFARS on JiuwenSwarm                      │
│                                                                   │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐       │
│  │ Ideation │──▶│ Planning │──▶│Experiment│──▶│ Writing  │──▶ PDF│
│  │  Swarm   │   │  Swarm   │   │  Swarm   │   │  Swarm   │  +SAR │
│  └────┬─────┘   └────┬─────┘   └────┬─────┘   └────┬─────┘       │
│       │   假设文档    │  实验契约     │  制品包       │ 蓝图+手稿    │
│  ─────▼──────────────▼──────────────▼──────────────▼──────────   │
│              共享工作区 workspace/（Git 版本化，制品留痕）          │
│  ────────────────────────────────────────────────────────────    │
│  JiuwenSwarm 增强层（= 我们的 PR 贡献）：                          │
│   • QualityGate 门控组件   • CircuitBreaker 熔断器                 │
│   • CheckpointManager 断点续跑  • MeteringMiddleware 资源计量     │
│   • AcademicFormatValidator 学术规范校验                            │
│  ────────────────────────────────────────────────────────────    │
│  基础设施：MaaS 模型路由（强/弱模型分级）│ 文献检索 API │ LaTeX 工具链│
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 在 JiuwenSwarm 上的落地方式

- 每个阶段实现为一个 **team skill**（进阶路线：通过 rail 拓展 harness 能力），四个阶段由顶层编排 skill `paper_pipeline` 串联。
- 阶段内部用 JiuwenSwarm 的蜂群子 Agent 做并行分工（如 Ideation 内的文献组/假设组/评审组）。
- 阶段间**不做同步消息传递**，通过共享工作区文件交接（借鉴 FARS 异步协作），天然解耦、可断点续跑。
- **必须修改 JiuwenSwarm 源码**的部分集中在 §7 的五个增强组件，以 PR 形式回馈社区。

### 4.3 数据流总览（单个论文项目）

```
topic.yaml（主题+约束+算力预算）
   │
   ▼ Ideation
workspace/proposals/*.md（N 个候选假设）──QualityGate──▶ accepted_proposal.md
   │
   ▼ Planning
workspace/plan/experiment_contract.yaml（五类任务分解）
   │
   ▼ Experiment
workspace/exp/{code, logs, results, checkpoints}/（逐任务提交）
   │   └─ 有效性评估门：不支持假设 → 早停，负结果保留
   ▼ Writing
workspace/paper/{blueprint.json, figures/, draft.tex, final.pdf}
   │
   ▼ 交付
paper.pdf + SAR Access Token + resource_report.md
```

---

## 5. 核心模块详细设计

### 5.1 Ideation Swarm（选题与假设生成）

**输入**：`topic.yaml`（三选一主题 + 子方向关键词 + 算力预算约束）。

**子 Agent 分工**（借鉴 FARS 的 Lead + Peer Discussion + Evaluation 结构）：

| 子 Agent | 职责 |
|---|---|
| SurveyAgent | 调用文献检索工具（arXiv API / Semantic Scholar），产出该子方向近 12 个月文献摘要卡片与"研究空白清单" |
| HypothesisAgent（Lead） | 基于空白清单生成 5~8 个候选假设，每个含：问题陈述、可验证预测、所需最小实验 |
| PeerAgent | 对候选假设做同行质询（新颖性检索查重、可行性打分），与 Lead 进行 1~2 轮辩论精炼 |
| GateAgent（Evaluation） | 按固定 rubric 打分：新颖性/可验证性/算力可行性/边界清晰度，≥ 阈值才放行 |

**FARS 机制移植**：
- **质量门**：只有过 GateAgent 的假设进入 Planning（FARS 部署中 244 假设 → 100 篇的转化率即来自此门）。
- **熔断器**：精炼迭代 ≥ K 轮（默认 3）仍无假设过门，自动选当前最高分草案放行，防止死循环。
- **域约束**：假设必须可全自动化验证（无需人工标注）、不超预算——写入 GateAgent 的硬性检查项。

**输出**：`workspace/proposals/accepted_proposal.md` + 被淘汰假设存档（供 innovation.md 与资源报告引用）。

### 5.2 Planning Swarm（实验契约生成）

**输入**：accepted_proposal.md。

**核心产物——experiment_contract.yaml**（直接移植 FARS 五类任务结构）：

```yaml
hypothesis: "..."           # 唯一中心假设
predict: "..."              # 可量化预测
tasks:
  env_setup:                # 环境搭建：数据/模型/依赖
    - id: E1 ...
  baselines:                # 基线实验：≥2 个已发表方法复现
    - id: B1 ...
  main:                     # 主实验：方法实现 + 对比
    - id: M1 ...
  effectiveness_gate:       # 有效性评估门：主结果是否支持假设
    threshold: ...
    on_fail: early_stop_keep_negative   # FARS 式早停但保留负结果
  analysis:                 # 分析实验：消融/敏感性/案例研究
    - id: A1 ...
budget: { max_gpu_hours: 8, max_tokens: 3000000 }
```

**设计要点**：
- 契约是**机器可读**的，Experiment Swarm 直接解析执行，消除自然语言计划的歧义；
- 强制包含 baselines ≥ 2 与 ablation ≥ 1——SAR 审稿中 "Experiments & Evaluation" 是最高频 weakness 类别（FARS 评审数据：92.9% 的评审提及），完备实验设计直接对冲 40 分的 SAR 评分；
- 预算字段与 MeteringMiddleware 联动，超预算任务自动降级或跳过。

### 5.3 Experiment Swarm（实验执行）

**定位**：Agent 方向的短论文实验以"轻量可复现"为纲——基准评测、小规模训练/微调、prompt/上下文工程对比、LLM-as-a-Judge 评估，全部可在单机 + MaaS API 完成。

**机制（逐条移植 FARS）**：

1. **实验循环**：Orchestrator 按契约顺序执行任务；每个任务 = 写代码 → 沙箱执行 → 收集指标 → 结构化落盘（results/*.json，供 Writing 程序化引用）。
2. **策展技能库** `skills/`：预置已验证模板——benchmark runner、多 seed 并行、绘图脚本（matplotlib 风格统一）、LLM-as-a-Judge 评分器。ExperimentAgent 只允许基于模板组合，禁止从零写评测逻辑，压缩错误率与 Token。
3. **有效性评估门**：main 完成后 GateCheck 对照契约阈值；不支持假设 → 跳过 analysis 节省资源，但 results 中完整记录负结果，Writing 阶段按 FARS "算法诚实" 原则写成 negative result 论文（短论文形式天然适配，且 SAR 对"可证伪的负结果"有明确好评案例，如 FARS 的 FA0328）。
4. **检查点容错**：每任务完成即 `git commit`（含结果与日志）；进程中断后 Orchestrator 扫描 checkpoint 从最后未完成任务续跑；终止处理器清理后台子进程。此组件实现为对 JiuwenSwarm 源码的修改（PR 点 3）。
5. **可审计性约束**：每个结果文件必须附带 `run_meta.json`（命令、seed、模型版本、token 消耗、时间戳）——决赛复现审核与诚信检查的防线。

### 5.4 Writing Swarm（证据优先写作）

**FARS 最核心的可借鉴设计：证据分析与行文分离。**

```
exp 制品包 ──▶ AnalysisAgent ──▶ blueprint.json ──▶ DraftAgent ──▶ draft.tex
                    │                                    │
                    └─ 图表生成（绘图脚本执行 + 示意图）    └─ FormatAgent 校验 ──▶ final.pdf
```

**Step 1 证据审计（AnalysisAgent）**：
- 审计 proposal / contract / results 三类制品，产出 `blueprint.json`：每个拟写入论文的 claim 必须显式链接到 {source_artifact, figure/table 候选, 支撑强度}；
- **无证据支撑的 claim 不允许进入蓝图**——结构性杜绝结果幻觉与过度声称（FARS 评审中 overclaiming 是高频失效模式，也是 SAR 扣分点）；
- 同步生成图表：分析图由绘图脚本在 results 上执行生成（保证图数=真实数据），方法示意图由图注描述生成。

**Step 2 起草（DraftAgent）**：
- 按 ICLR 模板结构（Abstract/Intro/Related Work/Method/Experiments/Analysis/Conclusion）逐节写作，每节输入仅为蓝图中对应证据；
- 相关工作引用只允许来自 SurveyAgent 产出的文献卡片（含真实 arXiv ID），引用前由 CitationChecker 校验条目存在性——针对 SAR 诚信审计中的 "hallucinated citations" 失效模式。

**Step 3 校验（FormatAgent）**：
- LaTeX 编译检查、ICLR 模板合规、摘要/关键词规范、参考文献格式（BibTeX）校验；
- **一致性审计**（移植 FARS 的 internal-consistency 检查）：扫描摘要/正文/表格中同一数值是否一致，防止不同 run 的数值混用——这是 FARS 八类诚信失效模式之一，SAR 评测同样会抓。

### 5.5 评审预演环（SAR 导向的最后一公里）

写作完成后进入**预评循环**（这是把 40 分 SAR 评分最大化的关键闭环）：

1. 用 SAR 平台（持有 Access Token）对 draft 进行预评，拉回评分与分项意见；
2. RebuttalAgent 解析意见，只处理**低风险高收益**项（补实验说明、修图表、调 claim 强度），不重写方法；
3. 最多迭代 2 轮（防过拟合评审器），随后定稿 → 正式 SAR 评测 → 记录 Access Token。

**限流策略**：SAR 每日评测有上限，预评请求按"凌晨错峰 + 本地队列"调度，决赛前 3 天锁定正式评测窗口。

---

## 6. 共享工作区与资源管理设计

### 6.1 workspace/ 目录规范（制品留痕 = 可审计性）

```
workspace/<project_id>/
├── topic.yaml
├── proposals/          # 全部候选假设（含淘汰的）
├── plan/experiment_contract.yaml
├── exp/
│   ├── code/           # 实验代码（每任务一 commit）
│   ├── logs/           # 运行日志
│   ├── results/        # *.json + run_meta.json
│   └── checkpoints/
├── paper/
│   ├── blueprint.json  # claim↔证据映射
│   ├── figures/
│   ├── draft.tex
│   └── final.pdf
└── metering/           # token/时长逐条流水
```

项目目录初始化即 `git init`，所有阶段提交都进版本库——决赛复现审核时可直接出示完整轨迹（FARS 在 GitLab 上做的，我们在本地等价实现）。

### 6.2 资源管理（对应 10 分资源消耗项）

| 策略 | 说明 | 借鉴 FARS 点 |
|---|---|---|
| 分级模型路由 | 文献摘要/格式校验用轻量模型；假设生成/写作用强模型 | 统一模型访问端点的分级版 |
| 上下文压缩 | 文献卡片摘要化后才进入下游；跨阶段只传制品路径不传全文 | 共享文件系统异步协作 |
| 技能库复用 | 评测/绘图代码模板化，避免重复生成 | 策展技能库 |
| 早停省算力 | 有效性评估门触发即跳过 analysis | FARS effectiveness gate |
| MeteringMiddleware | 每次 LLM 调用记录 {阶段, agent, in_tokens, out_tokens, latency, cost}，落盘 metering/ | FARS 资源可审计 |

`resource_report.md` 由脚本从 metering/ 流水**自动生成**（阶段×Agent 的 token 明细表、阶段时长、总成本），保证"可追溯"要求且零人工誊抄误差。

---

## 7. openJiuwen 框架贡献设计（15 分专项）

以下五个组件全部实现为**对 JiuwenSwarm 源码的修改**，每个单独成 PR，提高合并率：

| # | PR | 内容 | 通用性（评审关注点） |
|---|---|---|---|
| 1 | QualityGate 门控组件 | 阶段出口可插拔评审门：rubric 配置化、阈值可设、评审记录落盘 | 任何 swarm 流水线都需要阶段质检 |
| 2 | CircuitBreaker 熔断器 | 迭代超限/连续失败自动降级（取最优草案/跳过任务） | 防长时运行失控，生产级刚需 |
| 3 | CheckpointManager | 任务级 checkpoint + git 提交钩子 + 断点续跑调度 | 支撑小时级长任务 |
| 4 | MeteringMiddleware | token/时长/成本统一计量中间件 + 报表生成器 | 所有 MaaS 场景通用 |
| 5 | AcademicFormatValidator | LaTeX 编译检查、ICLR 模板合规、引用存在性校验、数值一致性扫描 | 科研场景生态扩展 |

**PR 策略**：
- 每个 PR 附完整单测 + 文档 + 非科研场景示例（证明通用性）；
- 分支命名 `feature/quality-gate` 等，PR 描述引用本比赛背景；
- 若合并窗口赶不上初赛轮次，`framework_contribution.md` 中同时附 patch 文件（规则允许"PR链接/patch文件等"）。

---

## 8. 论文产出策略（冲 60 分论文质量）

### 8.1 主题选择

三个建议主题与 FARS 部署数据的交叉判断：

| 主题 | 优势 | 风险 | 建议 |
|---|---|---|---|
| Agent 上下文工程 | 实验轻（prompt/检索对比即可），易出完备实验 | 竞争激烈、新颖性难做 | 第一轮主打 |
| Agent 记忆引擎 | 可做消融（记忆组件增删），实验结构天然完备 | 评测需自建 benchmark | 第二/三轮 |
| Agent 自演进 | 前沿、SAR 可能给高新颖性 | 实验重、易 overclaim | 谨慎，作为差异化备选 |

策略：利用"五轮取最高分"规则，每轮投不同主题/假设的论文，实测 SAR 分数后收敛到最优方向。

### 8.2 针对 SAR 评分项的写作规范

基于 FARS 282 份人类评审的统计结论反推 SAR 关注点：

1. **Contribution 是总分最强预测因子（r=0.743）** → 摘要与引言必须在前两段内讲清"唯一中心贡献是什么、为何此前没人做"；
2. **Soundness 次之（r=0.652）** → 实验必须有 baselines ≥ 2、多 seed、显著性说明；
3. **实验类 weakness 在高低分论文中都 100% 出现** → 主动在 Limitations 节坦白实验边界（FARS 高分论文的共同特征：candid analysis）;
4. **Presentation 最易得分（均值最高）** → 交给 FormatAgent + ICLR 模板保证下限；
5. **诚信红线**：引用必须真实存在、数值必须与 results 一致、方法与代码必须一致——这三条在本地做静态审计，对应 FARS 八类诚信失效模式的自动检查。

### 8.3 长文与跨主题泛化准备（决赛 20 分项）

- topic.yaml 全参数化：换主题 = 换配置文件，流水线零改动；
- 长文模式：`--mode long` 时 Writing Swarm 切换为"分节多轮起草 + 全局一致性复审"策略，蓝图按 section 分组扩展；
- 9 月轮次中专门安排一次"跨主题 + 长文"演练，实测泛化能力。

---

## 9. docs 与提交物撰写映射

设计文档到提交物的映射关系（写作分工提前定死）：

| 提交物 | 内容来源 |
|---|---|
| `docs/architecture.md` | 本文档 §4 + §5 精简版（含架构图） |
| `docs/module_call.md` | §4.2 + §7：列出继承的 JiuwenSwarm 基类、注册的工具、调用的内置模块 |
| `docs/innovation.md` | 三点：①FARS 式可审计四阶段流水线首次落地 JiuwenSwarm ②证据优先写作（blueprint claim↔证据映射）③资源计量+熔断的自限流设计 |
| `framework_contribution.md` | §7 表格 + 真实 PR 链接 |
| `resource_report.md` | §6.2 MeteringMiddleware 自动生成 |
| `提交说明.md` | §2.2 目录树 + 各文件一句话说明 |
| `code/run_guide.md` | 复现指南：环境→config→`python code/main.py --topic agent_context --mode short` |

---

## 10. 实施排期（对齐五轮提交窗口）

| 时间窗 | 里程碑 | 提交目标 |
|---|---|---|
| 即刻 ~ 07/31 前剩余时间 | JiuwenSwarm 环境跑通；四阶段骨架 + 共享工作区；PR1/PR2 提交 | 第一轮已过，若第一轮未提交则以第二轮为首战 |
| 08/01 ~ 08/20（当前轮） | Experiment Swarm + 技能库 + 有效性门；写作链 v1；主题 A（上下文工程）出第一篇论文 | **第二轮提交：完整流水线 v1 + 主题 A 论文** |
| 08/21 ~ 09/10 | 预评循环（SAR 接入）+ FormatAgent 一致性审计；PR3/PR4/PR5；主题 B | 第三轮：SAR 导向迭代版论文 |
| 09/11 ~ 09/30 | 长文模式 + 跨主题泛化演练；负结果论文实验 | 第四轮：最优主题定稿 |
| 09/31 ~ 10/09 | 定稿冲刺：正式 SAR 评测（提前 ≥2 天）、全部文档冻结、复现演练 | 第五轮：最终提交包 |
| 10/09 ~ 10/31 | 复现材料、决赛答辩、代码开源至 agent-store | 决赛 |

每轮内部节奏：截止前 3 天冻结代码 → 前 2 天完成 SAR 正式评测 → 前 1 天打包走查（对照 §2.2 清单）→ 截止日只处理意外。

---

## 11. 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| SAR 每日限流导致拿不到 token | 高 | 错峰队列 + 提前 2 天评测 + 预留备用评测日 |
| PR 未合并影响 15 分贡献项 | 中 | patch 文件兜底（规则明确允许）；多 PR 分散风险；主动在 discussions 区沟通 |
| 论文新颖性不足、SAR 分低 | 中 | 五轮多主题赛马；PeerAgent 查重前置；快速跟进新发表工作（FARS 的 3 天跟进能力靠 SurveyAgent 高频刷新实现） |
| 实验超预算/跑不完 | 中 | 契约预算字段硬约束 + 早停门 + 轻量实验设计原则 |
| 决赛复现失败 | 高 | run_meta.json 全量留痕；每轮提交后做 1 次干净环境复现 |
| LLM 引用幻觉/数值不一致 | 中 | CitationChecker + 一致性静态审计（§5.4 Step3），诚信问题在低分论文中高发（FARS 数据 43.5% vs 2.0%），是分数天花板的直接决定因素 |

---

## 附录 A：关键术语对照

| 本方案 | FARS 原型 | JiuwenSwarm 载体 |
|---|---|---|
| Ideation/Planning/Experiment/Writing Swarm | 同名四阶段 | team skill（rail 拓展） |
| workspace/ + git | 共享工作区 + GitLab namespace | 本地目录 + git |
| experiment_contract.yaml | Experiment Contract | 自定义 schema |
| blueprint.json | Paper Blueprint | 自定义 schema |
| MeteringMiddleware | 部署级资源审计 | JiuwenSwarm 源码新增 |
| GateAgent / CircuitBreaker | 自动质量门 / 熔断器 | JiuwenSwarm 源码新增 |

## 附录 B：参考资料

- FARS 论文：arXiv:2606.31651 (Tang et al., 2026, *FARS: A Fully Automated Research System Deployed at Scale*)
- FARS 深度研究报告：https://zhichai.net/htmlpages/FARS.html
- 赛题页：https://www.xir.cn/competition/1167 （含 dataware 评测页）
- openJiuwen 官网：https://openjiuwen.com/ ；仓库：https://atomgit.com/openJiuwen
- JiuwenSwarm 快速上手：https://atomgit.com/openJiuwen/jiuwenswarm/blob/develop/docs/zh/Quickstart.md
- Swarmskill：https://teamskills.openjiuwen.com/
- Stanford Agentic Reviewer：https://paperreview.ai
