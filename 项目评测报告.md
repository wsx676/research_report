# miniFARS 项目评测报告

> **评测时点**：2026-08-25 17:33（D1 全天任务完成后，D2 开工前）
> **评测对象**：工作区 `科研报告` 内全部产出（workspace 产出物、code/ 代码包、JiuwenSwarm PR1 分支、环境）
> **评测方法**：静态清点（git 留痕 / 文件制品 / 代码规模）+ 动态复测（重跑 dry-run、重跑 PR1 单测）+ 对照《方案设计文档.md》§1.3 评分维度与《开发计划.md》D1 验收标准
> **结论**：**D1 核心任务全部达成并实测复现通过；项目整体处于 5 天计划的 20% 时点，关键基础设施（共享工作区、计量中间件、质量门组件）已就绪，未发现阻塞性缺陷；主要风险集中在 PR 推送依赖外部账号与 D1 三项遗留验收。**

---

## 1. 动态复测结果（评测时点实测）

| 复测项 | 命令 | 结果 | 判定 |
|---|---|---|---|
| 流水线可复现性 | `python code/main.py --topic agent_context --dry-run` | 新项目 `agent_context-20260825-1733` 于 **2.8s** 完成四阶段，4 个 `[stage]` 提交（61fad3f/2da201d/192841b/be255a5），4 条编排流水落盘 | ✅ 通过 |
| PR1 组件质量 | `pytest tests/unit_tests/common/test_quality_gate.py` | **19 passed in 1.63s**（含熔断路径 3 项、记录落盘 3 项、非科研场景 1 项） | ✅ 通过 |
| 幂等续跑 | （历史观测：1707 项目先后两次运行） | 同项目重复运行追加 4 个新提交而不报错，git 历史完整（9 commits） | ✅ 通过 |
| 真实 LLM 链路 | （历史观测：1708 项目 `--smoke`） | 3 条真实 MiniMax-M2 调用（in=44，out=44/64/64）+ 流水先于提交落盘、随 `[stage]` 入库 | ✅ 通过 |

## 2. D1 验收标准对照（《开发计划.md》L44-47）

| # | 验收标准 | 状态 | 证据 |
|---|---|---|---|
| 1 | 官方示例跑通且能复述 skill/子 Agent 注册机制（写进 module_call.md 草稿） | ⚠️ 部分达成 | 服务四端口（18092/19000/19001/5173）监听验证通过；rail 回调体系（`AgentCallbackEvent`/`ModelCallInputs`）已调研确认为 PR2 挂点；**但 module_call.md 草稿未动笔** |
| 2 | `--dry-run` 在 workspace 留下四个阶段目录与 git 提交记录 | ✅ 达成 | 3 个项目目录、共 18 个 `[stage]` 提交 + 3 个 init 提交；四阶段目录（proposals/plan/exp/paper）与制品齐全 |
| 3 | metering/ 中至少 3 条真实调用流水 | ✅ 达成 | 1708 项目 3 条真实调用（hypothesis_lead/planner/drafter）+ 4 条编排流水；1707 项目另有 4 条（含修复前空文本记录，保留作审计痕迹） |

**晚间任务对照**：分支 `feature/quality-gate` ✅、QualityGate 组件接口与单测框架 ✅（提交 105c6a6，486 行新增，patch 备份 22KB）、topic.yaml ✅（含子方向关键词 + 算力预算三约束）。

## 3. 比赛评分维度完成度（§1.3 指挥棒）

| 评分维度 | 分值 | 当前完成度 | 依据与差距 |
|---|---|---|---|
| 论文质量：SAR 官方评分 | 40 | **0%**（符合 D1 预期） | 尚无 proposal/实验/论文；此维度依赖 D2-D4 产出 |
| 论文质量：评测 Agent 评分 | 20 | **0%**（符合 D1 预期） | SAR 预评流水线为 D4/D5 交付物 |
| Agent 系统能力 | 15 | **~40%** | 已有：四阶段编排可复现、git 全程留痕、质量门/熔断设计落地。缺：architecture.md、module_call.md、真实 Ideation 端到端证据 |
| 资源消耗 | 10 | **~70%** | 已有：MeteringMiddleware 上线且流水验证、分级路由配置就绪、上下文压缩红线（阶段间只传路径）。缺：resource_report.md 自动生成（D5）、light 档未真正接入第二模型 |
| openJiuwen 贡献 | 15 | **~35%** | 已有：PR1 组件完成（QualityGate + 熔断 + JSONL 评审记录 + 19 单测 + patch 兜底）。缺：**未推送到 fork/未提 PR**（依赖 atomgit 账号），PR2/PR3 未开工 |
| **若今日提交的预估总分** | 100 | **~18/100** | 系统能力 ~6 + 资源 ~7 + 贡献 ~5；**远未到可提交状态，符合 D1 时点预期，无需焦虑** |

## 4. 交付物清单与规模

### 4.1 代码包 `code/`（842 行，纯 Python 763 行）

| 文件 | 行数 | 职责 | 状态 |
|---|---|---|---|
| `minifars/metering.py` | 180 | MeteringMiddleware（token/时延/成本流水） | ✅ 已验证 |
| `minifars/pipeline.py` | 147 | paper_pipeline 四阶段编排 | ✅ 已验证 |
| `minifars/artifacts.py` | 127 | 制品 schema v0（proposals/results/run_meta/blueprint） | ✅ 已验证 |
| `minifars/workspace.py` | 94 | 工作区初始化 + git 提交规范 | ✅ 已验证 |
| `minifars/llm.py` | 84 | Anthropic 协议客户端（全调用过计量） | ✅ 已验证 |
| `minifars/config.py` | 69 | 三级 env 合并 + 分级模型路由 | ✅ 已验证 |
| `main.py` | 50 | CLI（dry-run/smoke/续跑） | ✅ 已验证 |
| `config.yaml` + `topics/agent_context.yaml` | 45 | 路由配置 + 主题 A | ✅ |

### 4.2 运行产出 `workspace/`（3 个独立 git 项目）

| 项目 | 用途 | 提交数 | 流水条数 |
|---|---|---|---|
| `agent_context-20260825-1707` | 首轮调试（含 thinking 坑现场） | 9 | 12（8 编排 + 4 真实） |
| `agent_context-20260825-1708` | **D1 正式验收项目** | 5 | 7（4 编排 + 3 真实） |
| `agent_context-20260825-1733` | 本次评测复测项目 | 5 | 4（全编排） |

1708 项目制品完整性抽查：`topic.yaml`、`proposals/P001.md`、`plan/experiment_contract.yaml`、`exp/results/t0_env.json` + `t0_env.run_meta.json`（复现五要素齐全）、`paper/blueprint.json`、`paper/draft.tex`、`metering/calls.jsonl` —— **§6.1 目录规范逐项在位**。

### 4.3 框架贡献（JiuwenSwarm `feature/quality-gate` 分支）

- 提交 `105c6a6`：`jiuwenswarm/common/quality_gate.py`（282 行）+ `tests/unit_tests/common/test_quality_gate.py`（204 行）
- patch 备份：`pr_patches/0001-feat-common-add-QualityGate-stage-exit-review-gate-P.patch`（22,255 字节，§7 兜底策略就位）
- 组件能力：配置化 rubric（加权归一）、阈值门、命名硬检查（挂域约束）、熔断器（连续 N 轮无过门强制放行最高分）、JSONL 评审记录（payload 默认不落盘）、科研四维预设 + 非科研示例（PR 通用性）

## 5. 资源消耗统计（metering 全量汇总）

| 指标 | 数值 |
|---|---|
| 流水总条数 | 23（编排 16 + 真实 LLM 7） |
| 真实 LLM 调用 | 7 次（ideation/planning/writing 各阶段 smoke + 1 次 debug probe） |
| 累计 token | 输入 308 / 输出 212 / 合计 **520 tokens**（占主题预算 1,500,000 的 **0.03%**） |
| 累计成本 | $0.00（MiniMax-M2 订阅制，单价记 0） |
| 平均真实调用时延 | ~1.1s（699~1532ms） |

> 判断：计量体系从 D1 第一条流水起持续在录，**满足"否则后四天资源数据缺失"的前置要求**；消耗远低于预算红线，D2 可放开跑真实 Ideation。

## 6. 环境快照

| 项 | 版本/状态 |
|---|---|
| Python | 3.11.16（conda env `JiuwenSwarm`） |
| workswarm | 0.2.5b1（可编辑安装） |
| openjiuwen | 0.1.17 |
| httpx / PyYAML / pytest | 0.28.1 / 6.0.3 / 9.1.1 |
| 密钥 | MiniMax-M2（Anthropic 协议）主用，智谱 EMBED 三件套就位 |
| JiuwenSwarm 服务 | 四端口启动验证通过（评测时已停止，可随时重启） |

## 7. 风险与差距清单（按优先级）

| # | 风险/差距 | 影响 | 缓解动作 |
|---|---|---|---|
| 1 | **PR1 未推送**：fork + push 依赖 atomgit 账号（wsx676）手动操作 | 贡献分（15 分）兑现链路未闭合 | patch 已备份可兜底；建议 D2 早晨完成 fork/push/PR 三步 |
| 2 | **module_call.md 草稿未写**（D1 遗留验收项 1 后半） | 系统能力分的文档链缺口 | D2 开工前补写：skill/子 Agent 注册机制 + rail 回调调研结论 |
| 3 | **文献检索未实测**：arXiv API / Semantic Scholar 尚无一次真实调用 | D2 SurveyAgent 是当日主线，若 API 不可用将阻塞 Ideation | D2 上午第一件事做连通性验证；备选：Semantic Scholar 限流则降级 arXiv 单源 |
| 4 | **LaTeX 工具链未验证**：ICLR 模板未编译过 | D4 写作阶段的风险后置 | D2-D3 间隙穿插验证，避免 D4 才发现缺 MiKTeX/Tectonic |
| 5 | **code/ 未纳入版本管理**：工作区根目录无 git 仓库 | 违反"每日总原则：代码进 Git"的字面要求（workspace 与 PR 分支已覆盖，源码本体无版本控制） | 根目录 `git init` + 首次提交（排除 workspace/、jiuwenswarm/ 大目录） |
| 6 | 分级路由两档同为 MiniMax-M2 | 资源优化策略未真正生效（当前成本 0，实际影响小） | D3 若 token 消耗逼近预算，接入智谱 GLM 作 light 档 |

## 8. 综合评价与下一步

**综合评价**：D1 的三条验收标准 2.5/3 达成（唯一缺口是 module_call.md 草稿）；骨架质量高于计划预期——dry-run 幂等复现、真实链路计量、PR1 组件带 19 项单测（已提前覆盖 D2 验收标准"熔断路径单测覆盖"）。520 tokens 的极小消耗换来全链路验证，资源纪律良好。**未发现任何阻塞性缺陷，D2 可按计划开工。**

**建议的 D2 开工顺序**（按风险前置原则）：
1. 补 D1 遗留：module_call.md 草稿 + arXiv/Semantic Scholar 连通性验证（阻塞项先排雷）
2. SurveyAgent 检索工具（近 12 个月 + 引用过滤，对应 topic.yaml 已备好的 search_filters）
3. 真实 Ideation：HypothesisAgent 产 5~8 假设 → PeerAgent 质询 → **QualityGate（今日交付的 PR1 组件）实岗打分** → accepted_proposal.md
4. 晚间：fork 推送 + PR1 正式提交（风险清单 #1 闭环）

---

*报告生成：miniFARS 开发助手 · 数据来源：git log / calls.jsonl / pytest / 复测运行，全部为实测值*
